import React from 'react'


const buttons = () => {
    <div>
        <Button variant="primary">Add a new Ingredient</Button>
        <Button variant="success">Submit</Button>
        <Button variant="danger">Remove  </Button>
    </div>

}